import * as React from 'react';

export const LandingPage = (props:any) => (
  <div>Welcome to the Landing Page . Test text.est text. est text. est text. est text. est text. est text. est text. est text. est text. est text. est text.  </div>
);